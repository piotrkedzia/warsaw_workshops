.jumbotron
  %h1 User profile page
  
  %p= user.lastname
  %p= user.firstname
  %p= user.email
  %p=link_to 'All user reviews', user_reviews_path
  

%h4= reviews.size<=5 ? "Recent user reviews" : "All user reviews"
%ul
  -reviews.each do |review|
    .panel-body
      %li="#{review.content}   #{review.created_at.strftime('%Y-%m-%d')}"
